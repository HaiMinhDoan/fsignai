package com.sunmoon.backend.constant.enums;

// Khớp CHECK constraint point_events.source trong V14__games_points_rewards.sql
public enum PointSource {
    GAME,
    QUIZ,
    PACK,
    LESSON,
    STREAK,
    DAILY_QUEST,
    AI_CHECK,
    MANUAL
}
